require "spec_helper"

describe LunaViewTool do
  it "has a version number" do
    expect(LunaViewTool::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
